- add images to profiles (thumbnail; click to see full image)
- generate image thumbnails in python
- add header to all pages:
	- breadcrumb
	- Name/logo?
	- User agent selector
		- Populate with available user agents when any page loads; store in local storage (when does local storage get cleared?)
- show crawl time/date
